## Summary
<!-- What does this PR change and why? -->

## Changes
-

## Checklist
- [ ] `make ci` passes (ruff, mypy, pytest+coverage, verify.py graph-state gate)
- [ ] Tests added/updated for the change
- [ ] If the corpus/detector changed: a near-miss guard keeps false positives at 0, and
      `make examples` was run (CI checks `examples/` is in sync)
- [ ] No recovered payload text is written back to the graph (hashes + safe labels only)
- [ ] Docs / README updated if needed

## Related Issues
Closes #
