# Environment variables
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Test / coverage / tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
coverage/
coverage.xml
htmlcov/

# Antigen runtime artifacts (live seeding)
.antigen-locus-map.json

# False-positive study corpus cache — 22 MB of third-party text we deliberately do
# NOT redistribute; regenerate with `python scripts/fp_corpus.py harvest`.
# Provenance + fingerprints are committed instead (docs/fp-corpus-*).
scripts/.fp_corpus_cache/

# Node (assets tooling only)
node_modules/

# OS
.DS_Store

# Vercel
.vercel

# Root-level asset kitchen only — the deployed site lives in web/public/ (tracked)
docs/assets