# Antigen dependencies.
#
# NOTE: the detector, the whole scan/cure engine, verify.py Part A, bench.py, and the
# entire test suite are Python STANDARD LIBRARY ONLY. You can run the full proof with
# zero installs:  `python verify.py`  and  `python -m antigen demo --offline`.
#
# The packages below are needed only for the LIVE path (a real DataHub GMS) and the
# victim-agent hijack demo (Part B):

# --- live DataHub path (scan/cure against a real GMS) ---
datahub-agent-context[langchain]==1.6.0.17
acryl-datahub==1.6.0.17

# --- victim-agent hijack demo (Part B, reported/non-gating) ---
langchain>=0.3
langchain-anthropic>=0.2      # or: langchain-openai (set ANTIGEN_DEMO_MODEL)
